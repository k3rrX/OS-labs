# Лабораторная работа №1 по Операционным Системам
## Вариант 6

### Студент: [Янкавцев Кирилл]
### Группа: [М8О-209БВ-24]

## Условие задачи
Родительский процесс создает дочерний процесс. Первой строчкой пользователь в консоль
родительского процесса вводит имя файла, которое будет использовано для открытия файла с
таким именем на чтение. Стандартный поток ввода дочернего процесса переопределяется
открытым файлом. Дочерний процесс читает команды из стандартного потока ввода.
Стандартный поток вывода дочернего процесса перенаправляется в pipe1. Родительский процесс
читает из pipe1 и прочитанное выводит в свой стандартный поток вывода. Родительский и
дочерний процесс должны быть представлены разными программами.

В файле записаны команды вида: «число число число<endline>». Дочерний процесс
считает их сумму и выводит результат в стандартный поток вывода. Числа имеют тип int.
Количество чисел может быть произвольным.

## Структура проекта
OS-labs/

└── lab1/

    ├── src/
    
    ├── doc/
    
    └── README.md

## Сборка и запуск

1. Перейдите в папку `src`:
   ```bash
   cd OSlab1/src

2. Скомпилируйте программы:
    make

3. Вернитесь в корневую папку:
cd ..

4. Запустите программу:
./src/parent

5. Когда программа запросит, введите имя файла с числами:
Родительский процесс: Введите имя файла с числами: test_file.txt

6. Ожидаемый вывод (сумма чисел из test_file.txt = 550):
Родительский процесс: Дочерний процесс создан (PID: 12345)
Родительский процесс: Читаю результат из pipe...
Родительский процесс: Получен результат: 550
Родительский процесс: Дочерний процесс завершился.

Использование strace для анализа
Для анализа системных вызовов выполните:
cd OSlab1/src
strace -f -o ../strace_output.txt ./parent

Затем введите имя файла. В файле strace_output.txt можно найти:
fork() или clone() - создание нового процесса
pipe() - создание канала
dup2() - перенаправление потоков


## **Как тестировать:**

1. **Перейди в папку проекта:**
   ```bash
   cd OSlab1/src

2. Собери программы:
make

3. Запусти родительский процесс из корня OSlab1:
cd ..
./src/parent

4. Введи имя файла:
test_file.txt

5. Если нужно создать свой тестовый файл, создай в корне OSlab1/ файл например my_numbers.txt:
1 2 3 4 5
10 20
100


И запусти с ним: ./src/parent, затем введи my_numbers.txt (сумма должна быть 145).
